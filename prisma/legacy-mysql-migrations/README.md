# Legacy MySQL migrations

These migrations are kept only as historical reference from the first version of the project.

The active database provider is PostgreSQL, and the official Prisma migration path is:

```txt
prisma/migrations/202606280001_postgresql_baseline
```

Do not run these legacy migrations against the current PostgreSQL database.
